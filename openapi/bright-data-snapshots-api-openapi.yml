openapi: 3.2.0
info:
  title: Bright Data Snapshots API
  version: v3
  contact:
    name: Bright Data
    url: https://docs.brightdata.com
  description: 'Operations tagged Snapshots across 2 of this provider''s published API definitions: bright-data-dataset-marketplace-api-openapi.yml, bright-data-web-scraper-api-openapi.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: https://api.brightdata.com
  description: Production
security:
- BearerAuth: []
tags:
- name: Snapshots
paths:
  /datasets/snapshots/{snapshot_id}:
    parameters:
    - name: snapshot_id
      in: path
      required: true
      schema:
        type: string
    get:
      summary: Get Dataset Snapshot
      operationId: getDatasetSnapshot
      tags:
      - Snapshots
      parameters:
      - name: format
        in: query
        schema:
          type: string
          enum:
          - json
          - ndjson
          - csv
          - parquet
      responses:
        '200':
          description: Snapshot rows.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
    servers:
    - url: https://api.brightdata.com
      description: Production
  /datasets/v3/log/{snapshot_id}:
    parameters:
    - name: snapshot_id
      in: path
      required: true
      schema:
        type: string
    get:
      summary: Get Snapshot Log
      operationId: getSnapshotLog
      tags:
      - Snapshots
      responses:
        '200':
          description: Snapshot log.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
    servers:
    - url: https://api.brightdata.com
      description: Production
  /datasets/v3/snapshots:
    get:
      summary: List Snapshots
      operationId: listSnapshots
      tags:
      - Snapshots
      parameters:
      - name: dataset_id
        in: query
        schema:
          type: string
      - name: from_date
        in: query
        schema:
          type: string
          format: date
      - name: to_date
        in: query
        schema:
          type: string
          format: date
      - name: status
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Snapshots.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Snapshot'
    servers:
    - url: https://api.brightdata.com
      description: Production
  /datasets/v3/snapshot/{snapshot_id}/cancel:
    parameters:
    - name: snapshot_id
      in: path
      required: true
      schema:
        type: string
    post:
      summary: Cancel a Snapshot
      operationId: cancelSnapshot
      tags:
      - Snapshots
      responses:
        '200':
          description: Cancelled.
          content:
            application/json:
              schema:
                type: object
    servers:
    - url: https://api.brightdata.com
      description: Production
  /datasets/v3/snapshot/{snapshot_id}/rerun:
    parameters:
    - name: snapshot_id
      in: path
      required: true
      schema:
        type: string
    post:
      summary: Rerun a Snapshot
      operationId: rerunSnapshot
      tags:
      - Snapshots
      responses:
        '200':
          description: Snapshot rerun started.
          content:
            application/json:
              schema:
                type: object
                properties:
                  snapshot_id:
                    type: string
    servers:
    - url: https://api.brightdata.com
      description: Production
  /datasets/v3/snapshot/{snapshot_id}:
    parameters:
    - name: snapshot_id
      in: path
      required: true
      schema:
        type: string
    get:
      summary: Download Snapshot Results
      operationId: downloadSnapshot
      tags:
      - Snapshots
      parameters:
      - name: format
        in: query
        schema:
          type: string
          enum:
          - json
          - ndjson
          - csv
          - jsonl
      - name: compress
        in: query
        schema:
          type: boolean
      - name: batch_size
        in: query
        schema:
          type: integer
      - name: part
        in: query
        schema:
          type: integer
      responses:
        '200':
          description: Snapshot data (up to 5 GB per request).
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
            text/csv:
              schema:
                type: string
            application/x-ndjson:
              schema:
                type: string
    servers:
    - url: https://api.brightdata.com
      description: Production
components:
  schemas:
    Snapshot:
      type: object
      properties:
        id:
          type: string
        dataset_id:
          type: string
        status:
          type: string
        created:
          type: string
          format: date-time
        dataset_size:
          type: integer
        records:
          type: integer
        errors:
          type: integer
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
x-refined-from:
- bright-data-dataset-marketplace-api-openapi.yml
- bright-data-web-scraper-api-openapi.yml
